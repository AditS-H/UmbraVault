# 🔥 HexStrike-Local: Complete Learning Guide

```
██╗  ██╗███████╗██╗  ██╗███████╗████████╗██████╗ ██╗██╗  ██╗███████╗
██║  ██║██╔════╝╚██╗██╔╝██╔════╝╚══██╔══╝██╔══██╗██║██║ ██╔╝██╔════╝
███████║█████╗   ╚███╔╝ ███████╗   ██║   ██████╔╝██║█████╔╝ █████╗  
██╔══██║██╔══╝   ██╔██╗ ╚════██║   ██║   ██╔══██╗██║██╔═██╗ ██╔══╝  
██║  ██║███████╗██╔╝ ██╗███████║   ██║   ██║  ██║██║██║  ██╗███████╗
╚═╝  ╚═╝╚══════╝╚═╝  ╚═╝╚══════╝   ╚═╝   ╚═╝  ╚═╝╚═╝╚═╝  ╚═╝╚══════╝
                    LOCAL PENTESTING FRAMEWORK
```

> **A secure, modular, AI-powered penetration testing framework with Docker sandboxing**

---

## 📑 Table of Contents

1. [What is HexStrike-Local?](#-what-is-hexstrike-local)
2. [Architecture Overview](#-architecture-overview)
3. [Core Components Deep Dive](#-core-components-deep-dive)
4. [Security Model](#-security-model)
5. [Tool Configuration System](#-tool-configuration-system)
6. [Docker Sandboxing](#-docker-sandboxing)
7. [API System](#-api-system)
8. [Terminal User Interface (TUI)](#-terminal-user-interface-tui)
9. [AI Integration](#-ai-integration)
10. [Data Flow & Execution Pipeline](#-data-flow--execution-pipeline)
11. [Error Handling & Validation](#-error-handling--validation)
12. [Reporting System](#-reporting-system)
13. [Setup & Configuration](#-setup--configuration)
14. [Extending the Framework](#-extending-the-framework)
15. [Security Best Practices](#-security-best-practices)
16. [Troubleshooting Guide](#-troubleshooting-guide)

---

## 🎯 What is HexStrike-Local?

### Overview

HexStrike-Local is a **local-only penetration testing automation framework** designed for ethical hackers and security researchers. It provides a secure way to run offensive security tools (like nmap, gobuster, sqlmap) inside **isolated Docker containers** while offering both an interactive terminal interface and a REST API.

### Key Philosophy

```
┌─────────────────────────────────────────────────────────────────────┐
│                    HEXSTRIKE DESIGN PRINCIPLES                      │
├─────────────────────────────────────────────────────────────────────┤
│  🔒 Security First    → All tools sandboxed in Docker containers    │
│  🧩 Modularity        → JSON-based tool configs, easy to extend     │
│  🤖 AI-Ready          → Optional Ollama integration for smart picks │
│  🏠 Local-Only        → Never exposes services to external networks │
│  ✅ Validation        → Strict input sanitization, no shell inject  │
│  📊 Reporting         → Automatic JSON reports with Rich previews   │
└─────────────────────────────────────────────────────────────────────┘
```

### What Problems Does It Solve?

| Problem | HexStrike Solution |
|---------|-------------------|
| Running pentest tools can be dangerous on host | Docker sandboxing isolates execution |
| Manual tool execution is tedious | Automated tool selection & chaining |
| No standardized output format | JSON reports with timestamps |
| Shell injection risks | Strict regex-based input sanitization |
| Remembering tool syntax | Pre-configured commands in JSON |
| Scattered tool installations | All tools in one Docker image |

---

## 🏗 Architecture Overview

### High-Level System Architecture

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           HEXSTRIKE-LOCAL ARCHITECTURE                        │
└──────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────────┐
                              │   USER INPUT    │
                              │  (IP / Domain)  │
                              └────────┬────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    ▼                  ▼                  ▼
           ┌───────────────┐  ┌───────────────┐  ┌───────────────┐
           │   TUI Mode    │  │   API Mode    │  │  Direct CLI   │
           │  (tui.py)     │  │ (server.py)   │  │   (future)    │
           └───────┬───────┘  └───────┬───────┘  └───────────────┘
                   │                  │
                   └────────┬─────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │   SANITIZER     │ ◄── Validates IP/Domain
                   │ (sanitizer.py)  │     Strips shell metachar
                   └────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │  TOOL SELECTOR  │ ◄── Maps task → tools
                   │  (selector.py)  │     Optional AI suggestions
                   └────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │    EXECUTOR     │ ◄── Docker container launch
                   │  (executor.py)  │     Timeout management
                   └────────┬────────┘
                            │
           ┌────────────────┼────────────────┐
           │                │                │
           ▼                ▼                ▼
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │   Docker    │  │   Docker    │  │   Docker    │
    │  Container  │  │  Container  │  │  Container  │
    │   (nmap)    │  │ (gobuster)  │  │  (sqlmap)   │
    └──────┬──────┘  └──────┬──────┘  └──────┬──────┘
           │                │                │
           └────────────────┼────────────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │    REPORTER     │ ◄── JSON output
                   │  (reporter.py)  │     Rich table preview
                   └────────┬────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │   logs/*.json   │
                   │   Output Files  │
                   └─────────────────┘
```

### Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      COMPONENT RELATIONSHIPS                     │
└─────────────────────────────────────────────────────────────────┘

    config.json ─────────────────┬──────────────────────────────┐
         │                       │                              │
         ▼                       ▼                              ▼
    ┌─────────┐            ┌──────────┐                 ┌──────────────┐
    │ server  │◄──────────►│ executor │◄───────────────►│   Docker     │
    │   .py   │            │   .py    │                 │  Container   │
    └────┬────┘            └────┬─────┘                 │ kalitools:   │
         │                      │                       │   latest     │
         ▼                      │                       └──────────────┘
    ┌─────────┐                 │
    │selector │◄────────────────┘
    │   .py   │
    └────┬────┘
         │
         ▼
    tools/*.json ──► network.json
                 ──► web.json
```

---

## 🔧 Core Components Deep Dive

### 1. Input Sanitizer (`src/sanitizer.py`)

The sanitizer is the **first line of defense** against malicious input. It validates all user-provided data before any tool execution.

#### How It Works

```python
# Validation Patterns
IP_REGEX    = r'^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}...$'
DOMAIN_REGEX = r'^[a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?...$'
PORT_REGEX   = r'^[1-9]\d{0,4}$'
```

#### Sanitization Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    INPUT SANITIZATION PIPELINE                    │
└──────────────────────────────────────────────────────────────────┘

     User Input: "192.168.1.1; rm -rf /"
                         │
                         ▼
              ┌──────────────────────┐
              │  Extract Target      │
              │  "192.168.1.1; rm..."│
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  IP Regex Match?     │──► NO ──► Domain Regex?
              │  ✅ 192.168.1.1      │              │
              └──────────┬───────────┘              │
                         │                         ▼
                        YES                ┌───────────────┐
                         │                 │  REJECT INPUT │
                         ▼                 │  ValueError   │
              ┌──────────────────────┐     └───────────────┘
              │  Strip Metacharacters│
              │  ; & | ` $ ( ) { }   │
              │  [ ] < >             │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Clean Output:       │
              │  "192.168.1.1"       │
              └──────────────────────┘
```

#### Blocked Characters

```
┌────────────────────────────────────────────────┐
│        BLOCKED SHELL METACHARACTERS            │
├────────────────────────────────────────────────┤
│  ;   → Command separator                       │
│  &   → Background execution                    │
│  |   → Pipe to another command                 │
│  `   → Command substitution                    │
│  $   → Variable expansion                      │
│  ()  → Subshell execution                      │
│  {}  → Brace expansion                         │
│  []  → Pattern matching                        │
│  <>  → Redirection                             │
└────────────────────────────────────────────────┘
```

---

### 2. Tool Selector (`src/selector.py`)

The selector maps task types to appropriate security tools and optionally uses AI to suggest additional tools.

#### Task-to-Tool Mapping

```
┌─────────────────────────────────────────────────────────────────┐
│                    TASK → TOOL MAPPING                          │
└─────────────────────────────────────────────────────────────────┘

    TASK TYPE          SELECTED TOOLS
    ─────────          ──────────────
    
    "network"  ───────► nmap
                   └──► rustscan (if available)
                   
    "web"      ───────► gobuster
                   ├──► nikto
                   └──► sqlmap
                   
    "custom"   ───────► (user-specified)
```

#### AI-Enhanced Selection (Optional)

```
┌─────────────────────────────────────────────────────────────────┐
│                 AI-ENHANCED TOOL SELECTION                       │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │  Base Selection │
                    │  nmap, gobuster │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Ollama Query   │
                    │  "Suggest 1     │
                    │   extra tool    │
                    │   for web scan" │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  AI Response:   │
                    │  "nuclei"       │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ Final Selection │
                    │ nmap, gobuster, │
                    │     nuclei      │
                    └─────────────────┘
```

---

### 3. Tool Executor (`src/executor.py`)

The executor is responsible for **safely running tools inside Docker containers** with proper resource limits and timeout handling.

#### Execution Modes

```
┌─────────────────────────────────────────────────────────────────┐
│                    EXECUTION MODES                               │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────┐
    │  MODE 1: Docker SDK (Preferred)                          │
    │  ─────────────────────────────                           │
    │  • Uses Python docker library                            │
    │  • Full container control                                │
    │  • Resource limits (512MB RAM)                           │
    │  • Automatic cleanup (remove=True)                       │
    └──────────────────────────────────────────────────────────┘
                              │
                              │ If SDK fails
                              ▼
    ┌──────────────────────────────────────────────────────────┐
    │  MODE 2: Docker CLI Fallback                             │
    │  ────────────────────────────                            │
    │  • Uses subprocess + docker run                          │
    │  • Bind-mounts logs directory                            │
    │  • Works in mixed WSL/Windows environments               │
    └──────────────────────────────────────────────────────────┘
                              │
                              │ If sandbox=false
                              ▼
    ┌──────────────────────────────────────────────────────────┐
    │  MODE 3: Local Subprocess (⚠️ UNSAFE)                    │
    │  ──────────────────────────────────                      │
    │  • Direct execution on host                              │
    │  • Only for development/testing                          │
    │  • NOT recommended for production                        │
    └──────────────────────────────────────────────────────────┘
```

#### Container Execution Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              DOCKER CONTAINER EXECUTION FLOW                     │
└─────────────────────────────────────────────────────────────────┘

    execute("nmap -sV {target}", "192.168.1.1")
                         │
                         ▼
    ┌────────────────────────────────────────┐
    │  1. Format Command                     │
    │     "nmap -sV 192.168.1.1"             │
    └────────────────────┬───────────────────┘
                         │
                         ▼
    ┌────────────────────────────────────────┐
    │  2. Create Docker Client               │
    │     Try: docker.from_env()             │
    │     Fallback: unix:///var/run/docker.. │
    └────────────────────┬───────────────────┘
                         │
                         ▼
    ┌────────────────────────────────────────┐
    │  3. Launch Container                   │
    │     image: kalitools:latest            │
    │     network: host                      │
    │     mem_limit: 512m                    │
    │     command: /bin/bash -lc "nmap..."   │
    └────────────────────┬───────────────────┘
                         │
                         ▼
    ┌────────────────────────────────────────┐
    │  4. Wait with Timeout                  │
    │     timeout: 300 seconds (default)     │
    │     If exceeded → container.kill()     │
    └────────────────────┬───────────────────┘
                         │
                         ▼
    ┌────────────────────────────────────────┐
    │  5. Collect Results                    │
    │     logs = container.logs()            │
    │     status = result['StatusCode']      │
    └────────────────────┬───────────────────┘
                         │
                         ▼
    ┌────────────────────────────────────────┐
    │  6. Return Structured Result           │
    │  {                                     │
    │    'success': true/false,              │
    │    'output': '...',                    │
    │    'elapsed': 12.5,                    │
    │    'error': null or 'message'          │
    │  }                                     │
    └────────────────────────────────────────┘
```

---

### 4. Reporter (`src/reporter.py`)

The reporter generates structured JSON reports and displays Rich-formatted tables to the console.

#### Report Structure

```json
{
  "timestamp": "2025-12-25T10:30:00+00:00",
  "task_type": "network",
  "results": [
    {
      "name": "nmap",
      "cmd": "nmap -sV -T4 192.168.1.1",
      "success": true,
      "output": "Starting Nmap 7.94...\n...",
      "elapsed": 45.2,
      "error": null
    },
    {
      "name": "rustscan",
      "cmd": "rustscan -a 192.168.1.1",
      "success": true,
      "output": "...",
      "elapsed": 8.1,
      "error": null
    }
  ],
  "summary": {
    "success_count": 2
  }
}
```

#### Console Output Preview

```
┌─────────────────────────────────────────────────────────────────┐
│                     NETWORK report                               │
├──────────────┬─────────────┬───────────────────────────────────┤
│ Tool         │ Status      │ Elapsed                           │
├──────────────┼─────────────┼───────────────────────────────────┤
│ nmap         │ ✅          │ 45.2s                             │
│ rustscan     │ ✅          │ 8.1s                              │
└──────────────┴─────────────┴───────────────────────────────────┘
```

---

## 🔐 Security Model

### Defense in Depth

```
┌─────────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                               │
└─────────────────────────────────────────────────────────────────┘

    LAYER 5: API Authentication
    ───────────────────────────
    │  JWT tokens required for all /run/* endpoints
    │  Secret key validation (min 32 chars)
    │
    ▼
    LAYER 4: Network Isolation  
    ──────────────────────────
    │  API binds to 127.0.0.1 ONLY
    │  No external network exposure
    │
    ▼
    LAYER 3: Input Validation
    ─────────────────────────
    │  Regex-based IP/domain validation
    │  Shell metacharacter stripping
    │
    ▼
    LAYER 2: Docker Sandboxing
    ──────────────────────────
    │  All tools run in containers
    │  Memory limits (512MB)
    │  Timeout limits (300s)
    │
    ▼
    LAYER 1: Resource Controls
    ──────────────────────────
       Read-only mounts for configs
       Automatic container cleanup
```

### JWT Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                 JWT AUTHENTICATION FLOW                          │
└─────────────────────────────────────────────────────────────────┘

    1. Generate Token
    ─────────────────
    python3 scripts/generate_token.py
    
    Output: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
    
    
    2. Make Authenticated Request
    ─────────────────────────────
    ┌────────────────────────────────────────────────┐
    │  POST /run/network                             │
    │  Headers:                                      │
    │    Authorization: Bearer eyJhbGci...           │
    │    Content-Type: application/json              │
    │  Body:                                         │
    │    {"target": "192.168.1.1"}                   │
    └────────────────────────────────────────────────┘
    
    
    3. Server Validation
    ────────────────────
    ┌──────────────────┐     ┌──────────────────┐
    │  Extract Token   │────►│  JWT Decode      │
    │  from Header     │     │  with SECRET     │
    └──────────────────┘     └────────┬─────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │                                   │
                    ▼                                   ▼
           ┌──────────────┐                    ┌──────────────┐
           │   Valid ✅   │                    │  Invalid ❌  │
           │  Continue    │                    │  401 Error   │
           └──────────────┘                    └──────────────┘
```

---

## 🛠 Tool Configuration System

### JSON Tool Definition Format

```
┌─────────────────────────────────────────────────────────────────┐
│                TOOL CONFIGURATION FORMAT                         │
└─────────────────────────────────────────────────────────────────┘

    tools/network.json
    ───────────────────
    {
      "nmap": {
        "cmd": "nmap -sV -T4 {target} -oN /app/logs/nmap_{target}.txt"
      },
      "masscan": {
        "cmd": "masscan -p1-65535 {target} --rate=1000"
      }
    }
    
    
    tools/web.json
    ──────────────
    {
      "gobuster": {
        "cmd": "gobuster dir -u {target} -w /usr/share/seclists/..."
      },
      "nikto": {
        "cmd": "nikto -host {target} -output /app/logs/nikto_{target}.txt"
      },
      "sqlmap": {
        "cmd": "sqlmap -u {target} --batch --output-dir=/app/logs/..."
      }
    }
```

### Template Variables

```
┌────────────────────────────────────────────────────────────────┐
│                  TEMPLATE VARIABLES                             │
├────────────────────────────────────────────────────────────────┤
│  {target}     → Replaced with sanitized IP/domain              │
│  /app/logs/   → Container path (bind-mounted to host logs/)    │
│                                                                │
│  Example:                                                      │
│  ─────────                                                     │
│  Template: "nmap -sV {target} -oN /app/logs/nmap_{target}.txt" │
│  Input:    target = "192.168.1.1"                              │
│  Result:   "nmap -sV 192.168.1.1 -oN /app/logs/nmap_192..."    │
└────────────────────────────────────────────────────────────────┘
```

### Available Tools in Docker Image

```
┌─────────────────────────────────────────────────────────────────┐
│               TOOLS INSTALLED IN KALITOOLS IMAGE                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  NETWORK RECONNAISSANCE                                         │
│  ─────────────────────                                          │
│  • nmap        - Network mapper, port scanner                   │
│  • masscan     - Fast port scanner                              │
│  • rustscan    - Fast port scanner (Rust)                       │
│  • amass       - Subdomain enumeration                          │
│  • theharvester- OSINT gathering                                │
│                                                                 │
│  WEB APPLICATION                                                │
│  ───────────────                                                │
│  • gobuster    - Directory/DNS brute-forcer                     │
│  • nikto       - Web server scanner                             │
│  • sqlmap      - SQL injection automation                       │
│  • ffuf        - Fast web fuzzer                                │
│  • dirsearch   - Web path scanner                               │
│  • nuclei      - Vulnerability scanner                          │
│                                                                 │
│  PASSWORD ATTACKS                                               │
│  ────────────────                                               │
│  • hydra       - Login brute-forcer                             │
│  • john        - John the Ripper password cracker               │
│  • hashcat     - Advanced password recovery                     │
│                                                                 │
│  UTILITIES                                                      │
│  ─────────                                                      │
│  • curl/wget   - HTTP clients                                   │
│  • netcat      - Network utility                                │
│  • whois       - Domain lookup                                  │
│  • dnsutils    - DNS tools                                      │
│  • seclists    - Wordlists collection                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🐳 Docker Sandboxing

### Container Configuration

```dockerfile
# docker/Dockerfile.tools
FROM kalilinux/kali-rolling:latest

RUN apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y \
    nmap gobuster sqlmap nikto hydra john hashcat \
    nuclei amass theharvester dirsearch ffuf \
    curl wget netcat-openbsd masscan whois dnsutils \
    seclists \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

WORKDIR /app
RUN mkdir -p /app/logs
CMD ["/bin/bash"]
```

### Runtime Container Settings

```
┌─────────────────────────────────────────────────────────────────┐
│              DOCKER RUNTIME CONFIGURATION                        │
└─────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────┐
    │  SETTING          │  VALUE           │  PURPOSE         │
    ├───────────────────┼──────────────────┼──────────────────┤
    │  image            │  kalitools:latest│  Tool container  │
    │  network_mode     │  host            │  Local scanning  │
    │  mem_limit        │  512m            │  Resource limit  │
    │  detach           │  True            │  Async execution │
    │  remove           │  True            │  Auto cleanup    │
    │  stdout/stderr    │  True            │  Capture output  │
    └───────────────────┴──────────────────┴──────────────────┘
    
    
    Volume Mounts (CLI Fallback):
    ────────────────────────────
    ┌──────────────────┐      ┌──────────────────┐
    │  Host            │      │  Container       │
    │  ./logs/         │ ───► │  /app/logs:rw    │
    └──────────────────┘      └──────────────────┘
```

### Container Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                 CONTAINER LIFECYCLE                              │
└─────────────────────────────────────────────────────────────────┘

    ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
    │ CREATE  │──►│  START  │──►│   RUN   │──►│  STOP   │
    └─────────┘   └─────────┘   └────┬────┘   └────┬────┘
                                     │             │
                               Timeout?        remove=True
                                     │             │
                                     ▼             ▼
                               ┌─────────┐   ┌─────────┐
                               │  KILL   │──►│ REMOVE  │
                               └─────────┘   └─────────┘
```

---

## 🌐 API System

### Endpoints Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    API ENDPOINTS                                 │
└─────────────────────────────────────────────────────────────────┘

    BASE URL: http://127.0.0.1:8888
    
    
    GET /health
    ───────────
    • No authentication required
    • Returns: {"status": "active", "local_only": true}
    • Use for: Health checks, connectivity tests
    
    
    POST /run/<task_type>
    ─────────────────────
    • Authentication: Bearer token required
    • Task types: "network", "web"
    • Request body: {"target": "IP or domain"}
    • Returns: {"results": [...], "report": "path/to/report.json"}
```

### API Request/Response Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                API REQUEST FLOW                                  │
└─────────────────────────────────────────────────────────────────┘

    CLIENT                                    SERVER
    ──────                                    ──────
    
    POST /run/network ────────────────────────►
    Authorization: Bearer eyJ...                │
    {"target": "192.168.1.1"}                  │
                                               │
                                    ┌──────────┴──────────┐
                                    │  1. Verify JWT      │
                                    │  2. Sanitize input  │
                                    │  3. Select tools    │
                                    │  4. Execute tools   │
                                    │  5. Generate report │
                                    └──────────┬──────────┘
                                               │
    ◄──────────────────────────────────────────
    200 OK
    {
      "results": [
        {"name": "nmap", "success": true, ...},
        {"name": "rustscan", "success": true, ...}
      ],
      "report": "./logs/network_20251225_103000.json"
    }
```

### cURL Examples

```bash
# Health check (no auth)
curl http://127.0.0.1:8888/health

# Network scan (with auth)
TOKEN=$(python3 scripts/generate_token.py | head -2 | tail -1)
curl -X POST http://127.0.0.1:8888/run/network \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"target": "192.168.1.1"}'

# Web scan (with auth)
curl -X POST http://127.0.0.1:8888/run/web \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"target": "http://target.local"}'
```

---

## 💻 Terminal User Interface (TUI)

### TUI Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│                    TUI WORKFLOW                                  │
└─────────────────────────────────────────────────────────────────┘

    ./run.sh tui
         │
         ▼
    ┌─────────────────────────────────────────┐
    │  🔍 Running preflight checks...         │
    │                                         │
    │  Checks:                                │
    │  • Config validation                    │
    │  • Docker availability                  │
    │  • Docker image exists                  │
    └─────────────────┬───────────────────────┘
                      │
         ┌────────────┴────────────┐
         │                         │
    Issues Found              No Issues
         │                         │
         ▼                         ▼
    ┌───────────────┐    ┌─────────────────────┐
    │ ⚠️ Warnings:  │    │  🚀 Ready           │
    │ • Issue 1     │    │                     │
    │ • Issue 2     │    │  ╭─────────────────╮│
    │               │    │  │ HexStrike TUI   ││
    │ Continue? y/n │    │  │ Enter 'quit'    ││
    └───────────────┘    │  ╰─────────────────╯│
                         └─────────────────────┘
                                   │
                                   ▼
                         ┌─────────────────────┐
                         │  Task type:         │
                         │  [network/web/quit] │
                         └─────────────────────┘
                                   │
                                   ▼
                         ┌─────────────────────┐
                         │  Target (IP/domain):│
                         │  > 192.168.1.1      │
                         └─────────────────────┘
                                   │
                                   ▼
                         ┌─────────────────────┐
                         │  Run? [y/n]         │
                         └─────────────────────┘
                                   │
                                   ▼
                         ┌─────────────────────┐
                         │  Running nmap...    │
                         │  Running rustscan...│
                         │                     │
                         │  ┌─NETWORK report─┐ │
                         │  │ nmap    ✅ 45s │ │
                         │  │ rustscan✅  8s │ │
                         │  └────────────────┘ │
                         └─────────────────────┘
```

### TUI Features

```
┌─────────────────────────────────────────────────────────────────┐
│                    TUI FEATURES                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ Preflight Validation                                        │
│     • Validates config.json before starting                     │
│     • Checks Docker daemon status                               │
│     • Verifies Docker image exists                              │
│     • Option to continue with warnings                          │
│                                                                 │
│  ✅ Interactive Prompts                                         │
│     • Task type selection (network/web/quit)                    │
│     • Target input with validation                              │
│     • Confirmation before execution                             │
│                                                                 │
│  ✅ Real-time Feedback                                          │
│     • Shows current tool being executed                         │
│     • Displays error hints immediately                          │
│     • Rich table summary on completion                          │
│                                                                 │
│  ✅ Error Hints                                                  │
│     • Wordlist not found → Install seclists                     │
│     • Connection timeout → Check target/timeout                 │
│     • Logs directory error → Verify bind mount                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🤖 AI Integration

### Ollama Integration (Optional)

```
┌─────────────────────────────────────────────────────────────────┐
│                 AI-ASSISTED TOOL SELECTION                       │
└─────────────────────────────────────────────────────────────────┘

    Requirements:
    ─────────────
    • Ollama installed locally
    • Model downloaded (llama3.2 recommended)
    • ai_model set in config.json
    
    
    config.json:
    ────────────
    {
      "ai_model": "llama3.2"   // Enable AI
      "ai_model": null         // Disable AI (default)
    }
    
    
    How It Works:
    ─────────────
    ┌─────────────────┐
    │  Base Selection │
    │  nmap, gobuster │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────────────────────────────────┐
    │  Prompt to Ollama:                          │
    │  "Suggest up to 1 extra tool name for web   │
    │   scan on target.com from: [nmap, gobuster, │
    │   nikto, sqlmap, nuclei, ffuf, ...]"        │
    └─────────────────────────────────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  AI Response:   │
    │  "nuclei"       │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ Final Selection │
    │ + nuclei added  │
    └─────────────────┘
```

---

## 🔄 Data Flow & Execution Pipeline

### Complete Request Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│              COMPLETE REQUEST LIFECYCLE                          │
└─────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │  PHASE 1: INPUT RECEPTION                                   │
    ├─────────────────────────────────────────────────────────────┤
    │                                                             │
    │  User Input                                                 │
    │      │                                                      │
    │      ├──► TUI: Prompt.ask("Target")                         │
    │      │                                                      │
    │      └──► API: request.get_json()['target']                 │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  PHASE 2: VALIDATION & SANITIZATION                         │
    ├─────────────────────────────────────────────────────────────┤
    │                                                             │
    │  InputSanitizer.sanitize_task({'target': '192.168.1.1'})    │
    │      │                                                      │
    │      ├──► IP_REGEX.match() OR DOMAIN_REGEX.match()          │
    │      │                                                      │
    │      └──► Strip: ; & | ` $ ( ) { } [ ] < >                  │
    │                                                             │
    │  Result: {'target': '192.168.1.1'} or ValueError            │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  PHASE 3: TOOL SELECTION                                    │
    ├─────────────────────────────────────────────────────────────┤
    │                                                             │
    │  selector.select_tools('network', '192.168.1.1')            │
    │      │                                                      │
    │      ├──► Map task_type → base tools [nmap, rustscan]       │
    │      │                                                      │
    │      ├──► Format commands with {target}                     │
    │      │                                                      │
    │      └──► (Optional) Query Ollama for suggestions           │
    │                                                             │
    │  Result: [                                                  │
    │    {'name': 'nmap', 'cmd': 'nmap -sV 192.168.1.1 ...'},     │
    │    {'name': 'rustscan', 'cmd': 'rustscan -a 192.168.1.1'}   │
    │  ]                                                          │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  PHASE 4: DOCKER EXECUTION                                  │
    ├─────────────────────────────────────────────────────────────┤
    │                                                             │
    │  FOR EACH tool:                                             │
    │      executor.execute(tool['cmd'], target)                  │
    │          │                                                  │
    │          ├──► docker.containers.run(                        │
    │          │        image='kalitools:latest',                 │
    │          │        command=['/bin/bash', '-lc', cmd],        │
    │          │        network_mode='host',                      │
    │          │        mem_limit='512m'                          │
    │          │    )                                             │
    │          │                                                  │
    │          ├──► container.wait(timeout=300)                   │
    │          │                                                  │
    │          └──► logs = container.logs()                       │
    │                                                             │
    │  Result: {                                                  │
    │    'success': True,                                         │
    │    'output': 'Nmap scan report...',                         │
    │    'elapsed': 45.2,                                         │
    │    'error': None                                            │
    │  }                                                          │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  PHASE 5: REPORTING                                         │
    ├─────────────────────────────────────────────────────────────┤
    │                                                             │
    │  reporter.generate_report('network', results)               │
    │      │                                                      │
    │      ├──► Create JSON structure with timestamp              │
    │      │                                                      │
    │      ├──► Write to logs/network_YYYYMMDD_HHMMSS.json        │
    │      │                                                      │
    │      └──► Print Rich table to console                       │
    │                                                             │
    │  Output: './logs/network_20251225_103000.json'              │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
```

---

## ⚠️ Error Handling & Validation

### Error Categories

```
┌─────────────────────────────────────────────────────────────────┐
│                    ERROR CATEGORIES                              │
└─────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────────┐
    │  CATEGORY 1: Input Validation Errors                       │
    ├────────────────────────────────────────────────────────────┤
    │  • Invalid IP format                                       │
    │  • Invalid domain format                                   │
    │  • Invalid port number                                     │
    │  • Shell metacharacters detected                           │
    │                                                            │
    │  Response: ValueError with clear message                   │
    │  HTTP: 400 Bad Request                                     │
    └────────────────────────────────────────────────────────────┘
    
    ┌────────────────────────────────────────────────────────────┐
    │  CATEGORY 2: Authentication Errors                         │
    ├────────────────────────────────────────────────────────────┤
    │  • Missing Authorization header                            │
    │  • Invalid JWT token                                       │
    │  • Expired token                                           │
    │                                                            │
    │  Response: 401 Unauthorized                                │
    └────────────────────────────────────────────────────────────┘
    
    ┌────────────────────────────────────────────────────────────┐
    │  CATEGORY 3: Execution Errors                              │
    ├────────────────────────────────────────────────────────────┤
    │  • Docker not available                                    │
    │  • Container timeout                                       │
    │  • Tool execution failure                                  │
    │  • Missing wordlist                                        │
    │                                                            │
    │  Response: Result with success=False, error=hint           │
    └────────────────────────────────────────────────────────────┘
    
    ┌────────────────────────────────────────────────────────────┐
    │  CATEGORY 4: Configuration Errors                          │
    ├────────────────────────────────────────────────────────────┤
    │  • Missing config.json                                     │
    │  • Invalid JSON syntax                                     │
    │  • Weak/missing secret_token                               │
    │  • Missing Docker image                                    │
    │                                                            │
    │  Response: Preflight check failure                         │
    └────────────────────────────────────────────────────────────┘
```

### Smart Error Hints

```
┌─────────────────────────────────────────────────────────────────┐
│                 SMART ERROR HINTS                                │
└─────────────────────────────────────────────────────────────────┘

    ERROR DETECTED                      HINT PROVIDED
    ──────────────                      ─────────────
    
    "No such file or directory"    →   "Logs directory not accessible 
    + "/app/logs" in output             in container. Verify bind mount."
    
    "does not exist"               →   "Wordlist not found. Install 
    + "wordlist" in output              seclists in Docker image."
    
    "connection timed out"         →   "Target unreachable or timeout
                                        too short."
    
    Docker SDK fails               →   Automatic fallback to CLI mode
                                        with bind-mounted volumes.
```

---

## 📊 Reporting System

### Report File Structure

```
logs/
├── network_20251225_103000.json    # Network scan results
├── network_20251225_110000.json
├── web_20251225_120000.json        # Web scan results
├── nmap_192.168.1.1.txt            # Raw tool output
├── gobuster_target.local.txt
└── nikto_target.local.txt
```

### Report JSON Schema

```json
{
  "$schema": "HexStrike Report v1.0",
  "timestamp": "ISO 8601 datetime",
  "task_type": "network | web",
  "results": [
    {
      "name": "tool name",
      "cmd": "full command executed",
      "success": "boolean",
      "output": "stdout + stderr",
      "elapsed": "seconds (float)",
      "error": "null or error message"
    }
  ],
  "summary": {
    "success_count": "number of successful tools"
  }
}
```

---

## ⚙️ Setup & Configuration

### Configuration File (`config.json`)

```json
{
  "api": {
    "host": "127.0.0.1",        // ⚠️ Never change to 0.0.0.0
    "port": 8888,               // API port
    "secret_token": "..."       // Min 32 chars, generate with script
  },
  "docker": {
    "image": "kalitools:latest", // Docker image name
    "timeout": 300               // Max execution time (seconds)
  },
  "tools_path": "./tools/",      // Tool config directory
  "logs_path": "./logs/",        // Output directory
  "sandbox": true,               // ⚠️ Keep true for Docker isolation
  "ai_model": null               // Set to "llama3.2" for AI features
}
```

### Setup Commands

```bash
# 1. Clone and enter directory
cd /mnt/e/Hacking/hexstrike-local

# 2. Run automated setup
chmod +x setup-wsl.sh
./setup-wsl.sh

# 3. Verify installation
python3 scripts/check_deps.py        # Check dependencies
python3 src/validator.py             # Validate config
python3 scripts/check_tool_versions.py  # Show tool versions

# 4. Generate API token (if using API)
python3 scripts/generate_token.py

# 5. Run
./run.sh tui    # Interactive mode
./run.sh api    # API server mode
```

---

## 🔧 Extending the Framework

### Adding a New Tool

```
┌─────────────────────────────────────────────────────────────────┐
│                ADDING A NEW TOOL                                 │
└─────────────────────────────────────────────────────────────────┘

    STEP 1: Add to Dockerfile
    ─────────────────────────
    # docker/Dockerfile.tools
    RUN apt-get install -y \
        ... \
        NEW_TOOL \       # Add package name
        ...
    
    
    STEP 2: Rebuild Docker Image
    ────────────────────────────
    cd docker
    docker build -t kalitools:latest -f Dockerfile.tools .
    
    
    STEP 3: Add Tool Configuration
    ──────────────────────────────
    # tools/network.json (or web.json)
    {
      "existing_tool": {...},
      "new_tool": {
        "cmd": "new_tool --option {target} -o /app/logs/new_{target}.txt"
      }
    }
    
    
    STEP 4: Update Task Mapping (optional)
    ──────────────────────────────────────
    # src/selector.py
    mappings = {
        'network': ['nmap', 'rustscan', 'new_tool'],  # Add here
        'web': ['gobuster', 'nikto', 'sqlmap'],
    }
```

---

## 🛡️ Security Best Practices

```
┌─────────────────────────────────────────────────────────────────┐
│               SECURITY BEST PRACTICES                            │
└─────────────────────────────────────────────────────────────────┘

    ✅ DO:
    ──────
    • Keep sandbox: true in config
    • Use strong secret_token (64+ random hex chars)
    • Only scan targets you own or have permission
    • Run preflight checks before tasks
    • Review Docker image contents periodically
    • Keep tools updated (rebuild Docker image)
    
    
    ❌ DON'T:
    ─────────
    • Change API host from 127.0.0.1
    • Set sandbox: false in production
    • Use default/weak tokens
    • Scan external targets without permission
    • Expose the API to the network
    • Run as root unnecessarily
```

---

## 🔍 Troubleshooting Guide

### Common Issues

```
┌─────────────────────────────────────────────────────────────────┐
│               TROUBLESHOOTING GUIDE                              │
└─────────────────────────────────────────────────────────────────┘


    ISSUE: Docker not running
    ─────────────────────────
    Error: "Docker daemon not running"
    
    Fix:
    $ sudo service docker start
    $ docker ps  # Verify
    
    
    ISSUE: Docker image not found
    ─────────────────────────────
    Error: "Image kalitools:latest not found"
    
    Fix:
    $ cd docker
    $ docker build -t kalitools:latest -f Dockerfile.tools .
    
    
    ISSUE: Wordlist not found
    ─────────────────────────
    Error: "Wordlist not found. Install seclists..."
    
    Fix:
    1. Add 'seclists' to Dockerfile.tools
    2. Rebuild image
    3. Update tool configs to use /usr/share/seclists/...
    
    
    ISSUE: Permission denied on logs
    ────────────────────────────────
    Error: "Permission denied: /app/logs"
    
    Fix:
    $ chmod 777 logs/  # Allow container write access
    
    
    ISSUE: JWT token invalid
    ────────────────────────
    Error: "401 Unauthorized"
    
    Fix:
    1. Regenerate token: python3 scripts/generate_token.py
    2. Verify secret_token in config.json matches
    3. Check token hasn't expired
    
    
    ISSUE: Target validation failed
    ───────────────────────────────
    Error: "Invalid target: Must be IP or domain"
    
    Fix:
    • Use plain IP: 192.168.1.1 (not http://192.168.1.1)
    • Use plain domain: target.local (not http://target.local/)
    • For URLs with path, use the domain only
```

---

## 📚 File Reference

```
┌─────────────────────────────────────────────────────────────────┐
│                    FILE REFERENCE                                │
└─────────────────────────────────────────────────────────────────┘

    hexstrike-local/
    │
    ├── config.json              # Main configuration
    ├── config_no_docker.json    # Config for non-Docker mode
    ├── run.sh                   # Entry point script
    ├── setup-wsl.sh             # Automated installer
    ├── requirements.txt         # Python dependencies
    │
    ├── src/                     # Core Python modules
    │   ├── __init__.py
    │   ├── server.py            # Flask API server
    │   ├── tui.py               # Terminal UI
    │   ├── executor.py          # Docker execution engine
    │   ├── selector.py          # Tool selection logic
    │   ├── sanitizer.py         # Input validation
    │   ├── validator.py         # Config & preflight checks
    │   └── reporter.py          # Report generation
    │
    ├── tools/                   # Tool configurations
    │   ├── network.json         # Network tools (nmap, masscan)
    │   └── web.json             # Web tools (gobuster, nikto, sqlmap)
    │
    ├── docker/                  # Container definitions
    │   ├── Dockerfile.tools     # Kali tools image
    │   └── docker-compose.yml   # Compose configuration
    │
    ├── scripts/                 # Utility scripts
    │   ├── check_deps.py        # Dependency checker
    │   ├── check_tool_versions.py
    │   ├── generate_token.py    # JWT generator
    │   └── rebuild_docker.sh    # Image rebuild script
    │
    ├── tests/                   # Test suite
    │   ├── __init__.py
    │   └── test_sanitizer.py    # Sanitizer unit tests
    │
    └── logs/                    # Output directory
        ├── *.json               # Scan reports
        └── *.txt                # Raw tool outputs
```

---

## 🎓 Learning Checkpoints

```
┌─────────────────────────────────────────────────────────────────┐
│              LEARNING CHECKPOINTS                                │
└─────────────────────────────────────────────────────────────────┘

    □ Understand the architecture flow diagram
    □ Know how input sanitization prevents shell injection
    □ Understand why Docker sandboxing is important
    □ Can explain the JWT authentication flow
    □ Know how to add a new tool to the framework
    □ Understand the difference between TUI and API modes
    □ Can troubleshoot common Docker issues
    □ Know the security best practices
    □ Understand the report JSON structure
    □ Can run preflight checks and interpret results
```

---

<div align="center">

**Made for ethical hackers who value security and automation** 🔐

```
Remember: Only scan targets you own or have explicit permission to test.
```

</div>
